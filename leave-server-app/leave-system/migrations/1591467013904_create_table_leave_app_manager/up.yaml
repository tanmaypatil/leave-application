- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "leave_app"."manager"("id" serial NOT NULL, "mgr_name" text
      NOT NULL, "status" boolean NOT NULL, PRIMARY KEY ("id") );
  type: run_sql
- args:
    name: manager
    schema: leave_app
  type: add_existing_table_or_view
